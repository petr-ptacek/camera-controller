{
  "name": "camera-controller",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --config vite.config.js --port 3001",
    "build": "vite build --config vite.config.js",
    "serve": "vite preview --config vite.config.js"
  },
  "devDependencies": {
    "vite": "^2.6.4"
  },
  "dependencies": {
    "face-api.js": "^0.22.2",
    "mic-check": "^1.1.0"
  }
}
